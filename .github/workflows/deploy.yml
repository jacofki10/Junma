name: Deploy to A2 Hosting

on:
  push:
    branches:
      - master # Change this from 'main' to 'master'

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      # 1. Get the latest code
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      # 2. Setup Node.js (Updated to v20 for better stability)
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # 3. Install dependencies and Build (THE FIX IS HERE)
      - name: 🔨 Build Project
        run: |
          rm -f package-lock.json
          rm -rf node_modules
          npm install
          npm run build

      # 4. Upload to A2 Hosting (FTP)
      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          # The folder on A2 where your site lives (usually public_html)
          server-dir: ./studiosmundo.com/Junma
          # The folder Vue creates (do not change this)
          local-dir: ./dist/
          # dangerously-force-upload: true # Uncomment if you have sync issues
